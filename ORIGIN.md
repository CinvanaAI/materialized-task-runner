# Origin

Extracted from `Operations/task_runner*` in the historical Python Agent Foundry workbench. The original implementation proved package/workflow materialization and execution but also managed shared running directories and process termination. The public extraction preserves the central mechanism while using unique runs, explicit authorization, bounded execution, and durable evidence.
